import { resolve } from "path";

export default {
  "config": resolve(__dirname, "db", "config.js"),
  "models-path": resolve(__dirname, "models"),
  "migrations-path": resolve(__dirname, "migrations")
};